{
    "build": {
        "builder": "DOCKERFILE"
    },
    "deploy": {
        "startCommand": "php artisan migrate && /usr/bin/supervisord -c /etc/supervisord.conf",
        "healthcheckPath": "/health",
        "port": 80
    }
}
